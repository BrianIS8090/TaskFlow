# TaskFlow 

TaskFlow — это приложение для управления задачами, построенное на стеке:
- **Frontend:** React + Vite + TypeScript + Tailwind CSS
- **Backend/Auth:** Firebase (Firestore, Authentication)
- **Desktop:** Tauri (Rust)

## 🛠 Предварительные требования

Перед началом убедитесь, что установлены:
- [Node.js](https://nodejs.org/)
- [Rust](https://rustup.rs/) (для сборки Desktop версии)

---

## 🚀 Разработка (Локальный запуск)

### Desktop версия (Рекомендуется)
Запускает приложение в отдельном окне (как оно будет выглядеть у пользователя) с поддержкой нативных функций.

```bash
npm run tauri dev
```

### Web версия
Запускает приложение просто в браузере.
*Примечание: Google Auth в браузере работает через Popup, а не через редирект на localhost, но логика одна.*

```bash
npm run dev
```

---

## 📦 Сборка Desktop приложения (Windows .exe)

Чтобы создать установочный файл (`.exe`) для распространения:

1. **Обновите версию** (опционально):
   Откройте файл `src-tauri/tauri.conf.json` и измените поле `"version"`:
   ```json
   "version": "1.0.1"
   ```

2. **Запустите сборку**:
   ```bash
   npm run tauri build
   ```
   *Если возникает ошибка "cargo not found", убедитесь, что Rust добавлен в PATH или перезапустите терминал.*

3. **Найдите файл**:
   Готовый установщик будет лежать здесь:
   `src-tauri/target/release/bundle/nsis/TaskFlow_x.x.x_x64-setup.exe`

Этот файл можно отправлять другим пользователям.

---

## 🌐 Деплой Web-версии (На сервер)

Проект настроен для деплоя на **Firebase Hosting**.

### 1. Сборка веб-версии
Сначала нужно собрать оптимизированную версию сайта (без Rust/Tauri):

```bash
npm run build
```
Это создаст папку `dist` с готовыми файлами.

### 2. Деплой
Отправка файлов на сервер Firebase:

```bash
firebase deploy
```
*Если команда не найдена, установите инструменты Firebase: `npm install -g firebase-tools` и войдите через `firebase login`.*

После завершения вы получите ссылку вида: `https://taskflow-app-brian-01.web.app`

---

## 🔑 Настройка Google Auth

Для работы входа через Google:

1. **Web:** В Firebase Console добавьте домен вашего сайта в Authorized Domains.
2. **Desktop:** Используется Client ID и локальный сервер.
   - **Redirect URI:** `http://localhost:14205`
   - Этот адрес должен быть добавлен в Google Cloud Console для вашего OAuth Client ID.

Если пользователи видят ошибку **"Access blocked: App is testing"**, переведите приложение в статус **"Published"** в Google Cloud Console (раздел OAuth consent screen).
